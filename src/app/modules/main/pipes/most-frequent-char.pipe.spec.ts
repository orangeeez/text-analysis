import { MostFrequentCharPipe } from './most-frequent-char.pipe';

describe('MostFrequentCharPipe', () => {
  it('create an instance', () => {
    const pipe = new MostFrequentCharPipe();
    expect(pipe).toBeTruthy();
  });
});
