import { MostFrequentWordPipe } from './most-frequent-word.pipe';

describe('MostFrequentWordPipe', () => {
  it('create an instance', () => {
    const pipe = new MostFrequentWordPipe();
    expect(pipe).toBeTruthy();
  });
});
